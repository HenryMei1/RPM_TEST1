pipeline {
    agent { label '10.3.153.202'}      #指定在那台Jenkins节点上运行
    stages {
        stage('更新开始') {
            steps {
                echo '更新开始'
                sh 'mkdir -p /usr/local/clo/ven/heli'
            }
        }
    }
}
